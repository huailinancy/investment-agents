# Secrets — never commit real credentials
config/email_config.json

# State files (auto-generated, can be large)
state/ipo_seen.json
state/benchmark_scores.json

# Python cache
__pycache__/
*.pyc
*.pyo
.env
